#pragma once  // Ensure the header is included only once during compilation

// Declaration of add function
// Takes two integers as input and returns their sum
int add(int a, int b);

// Declaration of greet function
// Prints a greeting message to the console
void greet();
