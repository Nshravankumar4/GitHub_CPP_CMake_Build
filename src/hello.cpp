#include "hello.h"    // Include the header file which declares the getMessage() function

// Define the getMessage() function
// This function returns a simple string message
std::string getMessage() {
    return "Hello from CI/CD C++ project!";  // Return the message to be used in main.cpp or tests
}
